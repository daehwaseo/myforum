<div class="row">
  <div class="col-xs-12 col-sm-12 col-md-6 col-md-offset-3">
    <h2>계정 관리</h2>
    
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= devise_error_messages! %>
    
      <div class="form-group">
        <%= f.label :name, "이름" %><br />
        <%= f.text_field :name, autofocus: true, class: 'form-control' %>
      </div>
    
      <div class="form-group">
        <%= f.label :email, "이메일" %><br />
        <%= f.email_field :email, autofocus: true, class: 'form-control' %>
      </div>
    
      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <div>현재 이메일 확인을 기다리는 중 입니다: <%= resource.unconfirmed_email %></div>
      <% end %>
    
      <div class="form-group">
        <%= f.label :password, "비밀번호" %> <i>(비밀번호를 변경하시지 않으시면 비워두세요.)</i><br />
        <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
        <% if @minimum_password_length %>
          <br />
          <em>최소 <%= @minimum_password_length %>자 이상</em>
        <% end %>
      </div>
    
      <div class="form-group">
        <%= f.label :password_confirmation, "비밀번호 확인" %><br />
        <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control' %>
      </div>
    
      <div class="form-group">
        <%= f.label :current_password, "현재 비밀번호" %> <i>(회원 정보 수정을 위해서 현재 비밀번호가 필요합니다.)</i><br />
        <%= f.password_field :current_password, autocomplete: "off", class: 'form-control' %>
      </div>
    
      <div class="actions">
        <%= f.submit "확인", class: 'btn btn-primary' %>
      </div>
    <% end %>
    
    <h3>계정 삭제</h3>
    
    <p>서비스에 만족하지 않으십니까? <%= button_to "계정 삭제", registration_path(resource_name), data: { confirm: "정말 계정을 삭제하시겠습니까?" }, method: :delete, class: 'btn btn-danger' %></p>
    
    <%= link_to "취소", :back, class: 'btn btn-default active' %>
  </div>
</div>